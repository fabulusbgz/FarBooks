document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton1");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate1").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton2");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate2").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton3");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate3").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton4");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate4").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton5");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate5").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton6");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate6").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton7");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate7").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton8");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate8").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton9");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate9").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton10");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate10").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton11");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate11").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton12");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate12").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton13");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate13").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton14");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate14").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton15");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate15").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton16");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate16").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton17");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate17").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton18");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate18").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton19");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate19").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton20");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate20").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton21");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate21").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton22");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate22").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton23");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate23").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton24");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate24").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton25");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate25").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton26");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate26").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton27");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate27").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton28");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate28").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton29");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate29").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton30");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate30").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton31");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate31").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton32");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate32").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton33");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate33").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton34");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate34").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton35");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate35").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton36");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate36").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton37");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate37").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton38");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate38").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton39");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate39").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton40");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate40").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton41");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate41").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton42");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate42").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});

document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton43");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate43").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});
document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton44");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate44").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});
document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton45");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate45").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});
document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton46");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate46").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});
document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton47");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate47").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});
document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton48");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate48").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});
document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton49");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate49").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});
document.addEventListener("DOMContentLoaded", function () {
    let html = [{}];

    let addButton = document.getElementById("addButton50");
    let pdf = document.getElementById("pdf");

    addButton.addEventListener("click", function () {
        let template = document.getElementById("pdfplate50").innerHTML;
        let rendered = Mustache.render(template, { html: html });
        pdf.innerHTML = rendered;
    });
});
